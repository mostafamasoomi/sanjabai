FROM pgvector/pgvector:pg16

# pgvector official image already includes:
# - vector.so in $libdir
# - extension SQL + control files
# Just ensure extension is auto-created on first init.
RUN echo "CREATE EXTENSION IF NOT EXISTS vector;" > /docker-entrypoint-initdb.d/00-pgvector.sql
