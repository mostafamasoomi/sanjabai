import { dict } from '@/lib/i18n'

const FA = {
  title: 'مستندات endpoint‌ها',
  sampleRequest: 'نمونه درخواست:',
}

const EN: typeof FA = {
  title: 'Endpoint docs',
  sampleRequest: 'Sample request:',
}

export const endpointDocsSectionStrings = dict(FA, EN)
