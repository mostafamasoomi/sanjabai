import { dict } from '@/lib/i18n'

const FA = {
  searchPlaceholder: 'جستجو در حافظه...',
}

const EN: typeof FA = {
  searchPlaceholder: 'Search memory...',
}

export const memorySearchBarStrings = dict(FA, EN)
