# Local internal options for sanjhubai wazuh-agent
# Enable syscheck (file integrity monitoring)
syscheck.disabled=no

# Increase log queue size for high-volume log forwarding
remoted.queue_size=16384

# Enable debug logging temporarily for troubleshooting (0=normal, 1=info, 2=debug, 3=trace)
analysisd.debug=0
